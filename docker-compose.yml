version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: octocrm_postgres
    environment:
      POSTGRES_USER: octocrm
      POSTGRES_PASSWORD: octocrm123
      POSTGRES_DB: octocrm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: octocrm_redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  postgres_data: